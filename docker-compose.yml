version: "3"

services:
  jenkins:
    container_name: jenkins
    image: jenkins/jenkins:lts-alpine
    ports:
      - 80:8080
      - 50000:50000
    volumes:
      - "jenkins_data:/var/jenkins_home"
volumes:
  jenkins_data:
    driver: local
